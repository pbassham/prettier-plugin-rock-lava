It should format an inline shortcode and preserve its markup
{[ youtube id:'abc123' ]}

It should normalize spacing around an inline shortcode markup
{[ youtube id:'abc123' ]}

It should format an inline shortcode with no arguments
{[ kpis ]}

It should indent the children of a block shortcode
{[ panel title:'Hello' ]}
  <p>Some body</p>
  {% assign x = 1 %}
{[ endpanel ]}

It should indent nested block shortcodes
{[ panel title:'Outer' ]}
  {[ panel title:'Inner' ]}
    text
  {[ endpanel ]}
{[ endpanel ]}

It should format an inline shortcode inside an html attribute
<div class="{[ kpis ]}">x</div>
